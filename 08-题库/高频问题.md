# C++ 后端高频问题

抽题规则：每次随机选 20 题；简单题 30 秒给结论，中等题 3 分钟讲机制，项目/设计题 10 分钟画图。答完写下“缺少的证据”，不要只记标准答案。

回答后用 [Q1-Q105 答案索引](答案索引.md) 跳到对应详解，核对机制、边界与验证方法。原题号保持不变，可继续使用 `quiz.ps1` 抽题。

## C++ 语言与 STL

1. `const` 的顶层和底层分别约束什么？
2. `constexpr`、`consteval`、`constinit` 有什么区别？
3. 数组传参为什么会退化成指针？如何保留长度？
4. `sizeof` 作用于数组、指针、空类和多态对象时如何判断？
5. 未定义行为、实现定义行为和未指定行为如何区分？
6. 什么是对象生命周期？何时会产生悬空指针/引用？
7. `malloc/free` 与 `new/delete` 的职责差异？
8. 构造函数初始化列表和构造函数体的执行顺序？
9. Rule of 0/3/5 分别解决什么问题？
10. `std::move` 做了什么？移动后对象必须满足什么保证？
11. 完美转发中的转发引用和引用折叠是什么？
12. RVO/NRVO 何时发生？为什么返回局部对象通常不应手写 `move`？
13. 虚函数表是怎样支持多态的？哪些细节不是标准保证？
14. 为什么基类析构函数常需要 `virtual`？
15. 对象切片是什么？如何避免？
16. `dynamic_cast`、`static_cast`、`reinterpret_cast` 的风险边界？
17. `unique_ptr`、`shared_ptr`、`weak_ptr` 的所有权模型？
18. `make_shared` 与单独 `new` 的内存布局和异常安全取舍？
19. 如何解决 `shared_ptr` 循环引用？
20. `vector` 扩容的触发、复杂度和失效规则？
21. `reserve` 与 `resize` 的区别？
22. `unordered_map` 的桶、负载因子、rehash 和最坏复杂度？
23. map/set 为什么常用红黑树？何时选择哈希表？
24. `emplace_back` 是否总比 `push_back` 好？
25. erase 一个容器元素后哪些迭代器、指针、引用仍有效？
26. lambda 捕获引用的生命周期风险是什么？
27. 模板实例化、特化和 ODR 有什么关系？
28. 编译、汇编、链接分别做什么？
29. 静态库和动态库的链接/部署差异？
30. 如何用 ASan、UBSan、TSan 和 GDB 定位不同类别的问题？

## 并发与性能

31. data race 与一般 race condition 的差别？
32. 什么是 happens-before？如何建立跨线程可见性？
33. mutex、rwlock、自旋锁、信号量和条件变量分别适合什么场景？
34. 条件变量为什么要在 `while` 中检查谓词？
35. 如何设计有界阻塞队列的关闭和异常路径？
36. `lock_guard`、`unique_lock`、`scoped_lock` 如何选择？
37. 如何通过锁顺序避免死锁？死锁检测和超时有什么代价？
38. CAS 的 ABA 问题是什么？
39. `memory_order_relaxed/acquire/release/seq_cst` 如何取舍？
40. 线程池如何处理任务队列、停止、拒绝和工作线程异常？
41. 伪共享是什么？如何测量而不是猜测？
42. 如何区分 CPU、锁、内存和 I/O 瓶颈？

## 操作系统与 Linux

43. 进程与线程的地址空间和资源共享边界？
44. fork/exec/wait 的关系；什么是僵尸进程？
45. 用户态到内核态的切换成本来自哪里？
46. 上下文切换保存哪些状态，为什么会影响吞吐？
47. 虚拟内存、页表、TLB 和缺页异常如何协作？
48. 写时复制的触发和收益？
49. 堆、栈、mmap 区域各有什么特点？
50. 页缓存、mmap、direct I/O、fsync 的关系？
51. fd、file、inode、dentry 的关系？
52. 阻塞/非阻塞和同步/异步分别描述什么？
53. select、poll、epoll 的核心差异？
54. epoll LT 与 ET 的读写循环有什么不同？
55. 零拷贝到底减少了哪些复制或上下文切换？
56. 死锁四条件以及工程上的预防方法？
57. 如何用 `strace`、`lsof`、`ss`、`perf` 和 `/proc` 排查服务？

## 计算机网络

58. TCP 三次握手为什么不是两次？
59. SYN 队列和 accept 队列分别是什么？
60. TCP 四次挥手为什么通常需要四次？
61. TIME_WAIT 的作用是什么？为什么不能只看数量就判定故障？
62. CLOSE_WAIT 通常说明哪一端的问题？
63. TCP 序号、确认号、滑动窗口和重传如何协作？
64. 流量控制和拥塞控制的区别？
65. 快速重传、快速恢复和 RTO 的触发条件？
66. TCP 粘包的根因和 framing 方案？
67. 长连接、连接池、心跳、空闲超时如何配合？
68. HTTP/1.1、HTTP/2、HTTP/3 的队头阻塞差异？
69. HTTPS 握手中证书、密钥交换和对称加密分别负责什么？
70. DNS 递归、迭代、缓存和 TTL？
71. 非阻塞 connect 如何通过 epoll 判断成功或失败？
72. Reactor、Proactor、one loop per thread 分别强调什么？
73. 慢客户端如何造成服务端背压？
74. 如何用 `ss`、`tcpdump`、`curl -v` 建立网络故障时间线？

## 数据库与 Redis

75. B+ 树为什么适合数据库索引？
76. 聚簇索引和二级索引的区别，回表是什么？
77. 联合索引的最左前缀如何判断？
78. 覆盖索引、索引下推和索引条件下推有什么收益？
79. 哪些情况可能导致索引未被使用，如何用 EXPLAIN 验证？
80. ACID 分别由哪些机制共同保证？
81. 四种隔离级别对应哪些并发现象？
82. MVCC 的版本链和 Read View 如何判断可见性？
83. 当前读与快照读有什么差异？
84. 行锁、间隙锁、临键锁和意向锁的范围？
85. 数据库死锁如何检测、回滚和重试？
86. redo、undo、binlog 的职责和写入顺序？
87. 为什么大事务会影响锁、日志和复制？
88. 如何优化深分页、批量写和 N+1 查询？
89. Redis 的 string/hash/list/set/zset 分别适合什么场景？
90. Redis 单线程执行命令为何仍可能有并发与阻塞问题？
91. RDB、AOF、重写和故障切换的取舍？
92. 过期删除与内存淘汰有什么区别？
93. 缓存穿透、击穿、雪崩如何区分和治理？
94. 缓存与数据库双写不一致如何设计补偿？
95. 分布式锁如何防止误删、过期和主从切换下的安全问题？

## 系统设计与项目

96. 如何从需求、QPS、存储和带宽开始设计短链接服务？
97. 如何设计幂等接口和重复消息处理？
98. 消息队列为什么会重复、积压、乱序？如何应对？
99. 分片键如何选择，热点和再分片怎么办？
100. 如何设计限流、熔断、超时、重试和降级，避免级联故障？
101. 如何讲清一次项目中的真实 bug 和证据链？
102. 如何证明性能优化有效，而不是只报一个主观感受？
103. 项目中哪些是框架已有能力，哪些是自己的实现和验证？
104. 当前实现的限制是什么，下一步如何演进？
105. 如果线上出现延迟尖刺，五分钟内先看哪些指标和日志？

## 口述评分

| 分数 | 表现 |
| --- | --- |
| 0 | 只会关键词或背诵结论 |
| 1 | 能给定义，但机制和边界不清 |
| 2 | 能解释机制、复杂度和一个例子 |
| 3 | 能比较取舍、指出异常路径并给验证方法 |
| 4 | 能结合项目证据、指标和后续改进回答追问 |

