# 双项目面试学习材料质量校验

最终校验日期：2026-09-16。校验范围是学习资料的结构、可定位性、关键答案一致性和本地阅读页面。项目功能测试与学习者掌握度不在本报告的通过结论内。

## 内容数量

| 项目 | 主问题 | 附答案追问 | P0 | P1 |
| --- | --- | --- | --- | --- |
| RMDB 知识卡 | 28 | 84 | 18 | 10 |
| ITMS 知识卡 | 24 | 72 | 14 | 10 |
| 综合模拟 | 10 | 20 | 按模拟轮次练习 | 按模拟轮次练习 |
| 合计 | 62 | 176 | 32 张核心知识卡 | 20 张进阶知识卡 |

知识卡共 52 张，每张都有简历锚点、学习目标、必要概念、主问题、60 秒参考答案、三层追问答案、例子、误答、自测和源码依据。模拟另含两种时长的项目介绍及速记表。

## 校验结果

- 卡片编号连续，无重复或缺号；必需字段和三个追问答案均存在。
- 251 处本地文件链接存在，带行号的引用未越界；5 处外链仅作为已记录 remote 地址列出，没有把在线可达性称为已验证。
- 55 个被引用的项目文件已保存 SHA-256 指纹，便于日后判断证据是否发生改变。文件内容和凭证值没有复制到指纹记录中。
- 简历的两项目描述逐项映射到知识卡；复习评分、实际测试结果未代填。
- 桌面 1440×1000 与窄屏 390×844 页面已查看；搜索、章节筛选、收起、展开和空结果提示检查通过，窄屏无页面横向溢出，题目标题不被顶部工具栏遮住。
- 七章 Markdown 汇编为总册和单文件 HTML。HTML 的阅读、搜索与折叠不依赖外网，源码文件链接依赖本机目录仍然存在。

本地结构校验脚本输出 `errors: []`；阅读页面校验输出 `failures: []`。项目专属证据索引另有字段和引用检查记录。

## 技术复核与修正

正文按当前源码区分教学索引与完整 B+ 树、基础 redo/undo 与完整 ARIES、多索引局部补偿与完整原子性。ITMS 区分交通缓存回源与认证入口可用性、五次提示与完整锁号、单命令原子与复合操作原子、单 Key TTL 与双层缓存新鲜度。

独立复核修正了模拟稿中未经确认的读副本假设，以及保存后失效的无条件表述；均改用当前代码可见的时序和失败分支。风险是静态推演，未写成已经复现的运行故障。

## 可重跑检查

在本目录使用已配置的 Node.js 运行 `生成网页.mjs` 更新总册和 HTML，使用已配置的 Python 运行 `校验资料.py` 检查字段与引用。阅读页面检查脚本位于 evidence，需先在本目录启动仅监听 127.0.0.1:8765 的本地 HTTP 服务。

详细机器输出：`evidence/资料校验.json`、`evidence/源码引用指纹.json`、`evidence/阅读页面校验.json`。这些是交付检查记录，不能当作 RMDB 单测、ITMS 接口测试或实际学习成绩。
